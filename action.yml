name: 'coverage'
description: 'Generate code coverage for Cargo projects using cargo-tarpaulin'
inputs:
  tarpaulin-version:
    description: 'The version of cargo-tarpaulin to use'
    required: true
    default: '0.8.6'
runs:
  using: 'node12'
  main: 'dist/index.js'